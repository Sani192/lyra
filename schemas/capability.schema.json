{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lyra.example/schemas/capability.schema.json",
  "title": "Lyra Capability Schema",
  "description": "Documentation placeholder for the planned capability schema. It records purpose and expected ownership boundaries without defining implementation details.",
  "type": "object",
  "properties": {
    "purpose": {
      "type": "string",
      "description": "Human-readable purpose of this schema."
    },
    "plannedFields": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Fields to be specified during schema design."
    }
  },
  "additionalProperties": true,
  "x-documentationStatus": "Draft",
  "x-intendedUse": "Schema design and validation planning; do not treat as implementation-ready until promoted to Implementation Ready or Approved."
}
